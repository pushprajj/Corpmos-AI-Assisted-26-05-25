// src/components/Navbar.tsx
'use client';

import { useSession } from 'next-auth/react';
import Link from 'next/link';

export default function Navbar() {
  // Sidebar removed for dashboard. Navigation is now in the top navbars.
  return null;
}